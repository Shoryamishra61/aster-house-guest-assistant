# Aster House Guest Assistant - Requirement & Production Risk Traceability

| Requirement / Production Failure Mode | Risk Class | Subsystem Implementation | Automated Verification Test | Verifiable Evidence | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Grounded Hotel Knowledge FAQ** | Hallucination / Outdated facts | Lexical token retriever with alias boosting (`retriever.ts`) & claim verifier (`verifier.ts`) | Vitest: `retrieval.test.ts`, `chat.test.ts` (EV-01, EV-02, EV-03) | Check-in returns 3:00 PM; Pool hours 7 AM-10 PM; Breakfast fee $18 | **VERIFIED** |
| **Pure Capacity Room Suitability** | False availability promise | Pure room capacity filter (`availability.ts: findSuitableRooms`) | Vitest: `domain.test.ts` (EV-04), Playwright: `E2E-02` | Suitable rooms returned for 3 guests without claiming live availability | **VERIFIED** |
| **Deterministic Availability & Nightly Pricing** | Financial loss / booking dispute | Pure TypeScript date math and inventory calculation; integer `Money` minor units | Vitest: `domain.test.ts` (EV-06), `productionSubsystems.test.ts` | Multi-night calculations computed in cents; total calculated correctly | **VERIFIED** |
| **Timezone & Leap Year Math** | Shifted booking dates | Property-scoped timezone (`hotelTime.ts`) & UTC date-only math | Vitest: `productionSubsystems.test.ts` (TIME-01 to TIME-09) | Leap days and month transitions verified across property timezones | **VERIFIED** |
| **Multi-Turn Session Memory** | State loss / slot drift | `InMemorySessionStore` with slot merge overwrite semantics (`mergeAvailabilitySlots`) | Vitest: `chat.test.ts` (EV-07), Playwright: `E2E-02` | Dates preserved while party size updated in subsequent turn | **VERIFIED** |
| **Real PDF Ingestion & Validation** | Malformed/scanned files | Zero-dependency text stream PDF extractor (`pdfExtractor.ts`) | Vitest: `ingestion.test.ts` (PDF-01 to PDF-09) | Valid text PDFs parsed; encrypted & scanned files rejected with typed diagnostics | **VERIFIED** |
| **Knowledge Conflict Detection** | Contradictory hotel policies | Knowledge governance conflict detection engine (`knowledgeGovernance.ts`) | Vitest: `ingestion.test.ts` (PR-04) | Conflicting checkout hours detected; held in review without auto-publish | **VERIFIED** |
| **Document Prompt Injection Defense** | System hijack via upload | Untrusted document isolation in candidate facts | Vitest: `ingestion.test.ts` (PR-03) | Injection commands tagged with `[SUSPICIOUS INJECTION]`; cannot self-approve | **VERIFIED** |
| **Instant Rollback & Audit Log** | Stale / erroneous publication | Immutable version history with single-click rollback and audit recording | Vitest: `ingestion.test.ts` (PR-06), Playwright: `E2E-06` | Rollback switches active version in <100ms; audit trail recorded | **VERIFIED** |
| **Multi-Tenant / Property Isolation** | Cross-tenant data breach | Server-derived `propertyId` with tenant-scoped registries (`propertyConfig.ts`) | Vitest: `tenantIsolation.test.ts` (TENANT-01 to TENANT-04) | Queries, escalations, and feedback strictly partitioned by property | **VERIFIED** |
| **PII Redaction in Telemetry** | Privacy violation in logs | Regex sanitization filter for logs (`pii.ts`, `logger.ts`) | Vitest: `productionSubsystems.test.ts` (PR-14) | Credit cards, phones, and emails scrubbed to `[REDACTED_*]` before emission | **VERIFIED** |
| **Public API Rate Limiting & Idempotency** | DoS attack / duplicate clicks | In-memory token bucket rate limiter & idempotency deduplication | Vitest: `productionSubsystems.test.ts`, `route.ts` | 429 status returned with `Retry-After`; duplicate requests return cached result | **VERIFIED** |
| **Emergency Kill Switches & Degradation** | Cascade provider failure | Runtime feature flags (`featureFlags.ts`) for availability, phrasing, ingestion | Vitest: `securityHardening.test.ts` (PR-19), Playwright: `E2E-05` | System degrades gracefully to front desk guidance without crashing | **VERIFIED** |
| **Fail-Closed Ops Authorization** | Unauthorized policy mutation | Production authorization guard requiring `OPS_ADMIN_SECRET` (`api/ops/route.ts`) | Vitest: `securityHardening.test.ts` (PR-08, PR-09) | Mutation endpoints reject unauthenticated requests with 401/403 | **VERIFIED** |
| **Subtle Guest Feedback Loop** | Unobserved guest dissatisfaction | Thumbs up/down feedback widget with deduplication and reason taxonomy | Vitest: `productionSubsystems.test.ts`, Playwright: `E2E-04` | Rating captured with request correlation; repeated clicks update in-place | **VERIFIED** |
| **Human Escalation Lifecycle** | Dead-end guest inquiries | Formal state machine (`open -> acknowledged -> in_progress -> resolved`) | Vitest: `productionSubsystems.test.ts` (PR-16) | Out-of-scope inquiries generate structured front desk escalation records | **VERIFIED** |
| **Offline Zero-Key Local Evaluation** | Evaluator friction / API keys | Mock LLM client with deterministic intent routing and zero external dependencies | Vitest: all 9 test suites passing with `MOCK_LLM=true` | Evaluator experience runs cleanly via `npm install && npm run dev` | **VERIFIED** |
