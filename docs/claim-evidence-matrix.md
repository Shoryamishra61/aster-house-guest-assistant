# Production Claim Evidence Matrix

Every claim in the project documentation is audited below and classified as **PROVEN**, **PARTIALLY PROVEN**, **ARCHITECTURAL INTENT ONLY**, **UNPROVEN**, or **INCORRECT**.

| Claim | Code Evidence | Test Evidence | Runtime Evidence | Status |
| :--- | :--- | :--- | :--- | :--- |
| Claim | Code Evidence | Test Evidence | Runtime Evidence | Status |
| :--- | :--- | :--- | :--- | :--- |
| **Deterministic Availability Truth** | Pure TypeScript domain calculation in `src/server/domain/availability.ts`. Zero LLM tool authority. | Vitest: `tests/unit/domain.test.ts` (11 tests), Playwright: `tests/e2e/chat.spec.ts` (E2E-02). | Direct orchestrator responses return exact calculated rates & nights. | **PROVEN** |
| **Money Minor Unit Invariant** | `src/server/domain/money.ts` uses integer cents (`amountMinor: number`). Rejects non-integer amounts. | Vitest: `tests/unit/productionSubsystems.test.ts` (`preserves integer minor units and rejects non-integer cents`). | All pricing math executed in cents; formatted via Intl currency. | **PROVEN** |
| **Hotel Timezone Awareness** | `src/server/domain/hotelTime.ts` handles property-scoped timezone (`America/New_York`), leap year checks, and UTC date-only math. | Vitest: `tests/unit/productionSubsystems.test.ts` (`calculates stay nights immune to browser/DST time zone shifts`). | Business dates formatted as `YYYY-MM-DD` strings. | **PROVEN** |
| **Grounded LLM Generation & Verifier** | `src/server/retrieval/verifier.ts` checks output against retrieved facts with claim-level validation (`MONEY`, `TIME`, `AVAILABILITY`, `BOOKING`, `CONTACT`). | Vitest: `tests/unit/retrieval.test.ts` (7 tests), `tests/unit/credibilityHardening.test.ts` (`P0-9: Adversarial Verifier Checks`). | Ungrounded claims trigger deterministic summary fallback. | **PROVEN** |
| **Prompt & Document Injection Defense** | Intent classifier boundaries + document inspection in `knowledgeGovernance.ts`. | Vitest: `securityHardening.test.ts`, `productionSubsystems.test.ts`, Playwright: `E2E-03`. | Adversarial inputs routed to `out_of_scope` abstention. | **PROVEN** |
| **PII Redaction in Telemetry** | `src/server/security/pii.ts` sanitizes emails, phones, credit cards, auth tokens. | Vitest: `tests/unit/credibilityHardening.test.ts` (`P0-7: PII Sanitization at Logger Sink`). | `logger.ts` serializes only sanitized objects. | **PROVEN** |
| **Idempotency & Rate Limiting** | `src/server/security/rateLimiter.ts`, `src/app/api/chat/route.ts`. | Vitest: `productionSubsystems.test.ts`, multi-workload benchmark `tests/loadBenchmark.ts` (`PERF-LIMIT`). | In-memory token bucket + request deduplication cache. | **PROVEN (Local In-Memory)** / **ARCHITECTURAL INTENT (Distributed Redis)** |
| **Knowledge Governance & Rollback** | `src/server/domain/knowledgeGovernance.ts` hashes files, detects conflicts, supports CAS publish and instant rollback. | Vitest: `credibilityHardening.test.ts` (`KB-01`, `KB-02`, `KB-03`). | `/ops` interface shows version history and rollback button. | **PROVEN (In-Memory CAS)** / **ARCHITECTURAL INTENT (Durable DB Transaction)** |
| **Real PDF Parsing & Ingestion** | `pdf-parse` integration + TJ array extraction + encrypted/scanned image detection in `pdfExtractor.ts`. | Vitest: `tests/unit/credibilityHardening.test.ts` (`PDF-REAL-01` to `PDF-REAL-04`), `tests/unit/ingestion.test.ts`. | `/ops` upload parses multi-page and text PDFs. | **PROVEN (Text-based)** / **EXPLICIT LIMITATION (OCR for scanned images not included)** |
| **Multi-Property / Tenant Isolation** | `propertyId` derived exclusively by server configuration. LLM cannot override scope. | Vitest: `tests/unit/tenantIsolation.test.ts` (4 tests). | Cross-tenant data leakage blocked at domain and HTTP boundaries. | **PROVEN** |
| **Fail-Closed Production Configuration** | `src/server/config/systemConfig.ts` blocks unauthorized mock providers, demo auth, and malformed config in production. | Vitest: `tests/unit/credibilityHardening.test.ts` (`CONFIG-01` through `CONFIG-07`). | Prevents silent fallback to mock mode in production deployments. | **PROVEN** |
| **Emergency Kill Switches & Degradation** | `src/server/security/featureFlags.ts` manages toggles for availability, model phrasing, and ingestion. | Vitest: `productionSubsystems.test.ts` (`defaults to safe operational configuration and supports runtime toggle`). | Toggles active in `/ops` console; `/api/ready` reflects degraded state. | **PROVEN** |
| **Throughput & Capacity** | Pure async Next.js pipeline with in-memory state caching. | HTTP Load Benchmark: **107 to 207 req/s** across 5 distinct workloads (p50: 59-101ms). Direct orchestrator microbenchmark: **900+ req/s**. | Measured on single local Node process. Distributed clustering unverified. | **PROVEN (Local Single-Process)** / **UNVERIFIED (Distributed Production)** |

